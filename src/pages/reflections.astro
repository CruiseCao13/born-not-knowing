---
import { getCollection } from "astro:content";
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Main from "@/components/Main.astro";
import Footer from "@/components/Footer.astro";
import Card from "@/components/Card.astro";
import { getSortedPosts } from "@/utils/getSortedPosts";
import config from "@/config";

const posts = getSortedPosts(
  await getCollection(
    "posts",
    ({ data }) => data.category === "Reflections" && !data.draft
  )
);
---

<Layout
  title={`Reflections | ${config.site.title}`}
  description="Notes on learning, revision, mistakes, and clearer thinking."
>
  <Header />
  <Main
    pageTitle="Reflections"
    pageDesc="Notes on learning, revision, mistakes, and clearer thinking."
  >
    <ul>{posts.map(post => <Card {...post} />)}</ul>
  </Main>
  <Footer />
</Layout>
